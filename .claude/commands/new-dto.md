Create a DTO (Data Transfer Object) for the use case described in $ARGUMENTS. Ask for the domain and fields if not specified.

Rules:
1. Place the file at `src/[domain]/dto/[create|update|query]-[domain].dto.ts`
2. Use `class-validator` for all validation decorators — the global `ValidationPipe` applies `whitelist: true, transform: true`
3. Never include `userId`, `id`, or `createdAt` in request DTOs — these come from the JWT or are generated by TypeORM
4. For update DTOs, extend `PartialType(CreateDto)` from `@nestjs/mapped-types` rather than duplicating fields
5. For nullable/optional fields: use both `@IsOptional()` and the appropriate type validator, with TypeScript type `string | null | undefined`
6. Numeric fields must use `@IsNumber()` and `@Type(() => Number)` (from `class-transformer`) to handle string-to-number coercion from JSON
7. Enum fields must use `@IsEnum(EnumType)` with the actual enum, not a plain string union
8. Add a JSDoc comment above the class briefly describing its purpose
